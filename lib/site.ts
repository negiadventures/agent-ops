/**
 * Where this app is actually served.
 *
 * Kept in one place because it has to agree in four: the canonical link, the
 * Open Graph URL, the sitemap and the robots file. When those disagree, Search
 * Console reports the page as a redirect or a duplicate and the report is
 * right.
 */
export const SITE_URL = "https://agentops.negiventures.com";
export const SITE_NAME = "Agent Ops";
